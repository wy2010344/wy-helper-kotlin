# AGENTS.md — AI 协作规范

本文件为 AI 编码助手（如 Trae、Cursor 等）提供项目协作规范。在本仓库工作时，请遵守以下规则。

---

## 一、语言

- **尽量用中文回复**，包括代码中的注释、commit message（如有）、以及与用户的交互。

---

## 二、先评估，再动手

执行任何指令前，**先评估可行性与潜在问题**，说明后再动手，不要硬上、不要盲目照做。评估维度包括但不限于：

- **工具链约束**：项目使用 Kotlin Multiplatform + Gradle + Skia（skiko），注意 JVM / Android / Web 三端差异，不要引入与现有工具链冲突的库。
- **环境差异**：注意不同平台（`commonMain` / `jvmMain` / `androidMain` / `webMain`）的可见性与 API 差异。
- **对架构的影响**：本项目采用模块化设计（`skia-engine` / `layout` / `signal` / `mve` / `lib`），改动前思考是否破坏了模块边界和依赖方向。

---

## 三、不要用歪门方法绕问题

遇到问题时，**不要使用以下规避手段**：

- 不要通过检测编译输出、改配置绕过等方式"蒙混过关"。
- 不要在不理解原理的情况下强行 try-catch、硬编码、复制粘贴现有代码来"解决"问题。

正确的做法：

1. **缺什么先搜 skills**：使用 `npx skills find <关键词>` 搜索可用的技能（skills）。
2. **找不到再问用户**：搜索无果时，向用户说明情况并请求指导。

---

## 四、代码风格一致性

- **遵循项目现有风格**：新增代码必须与同文件、同模块的风格保持一致（命名方式、缩进、空格、括号风格等）。
- 参考同模块已有代码的写法，不要引入个人偏好的格式。
- Kotlin 代码遵循 [Kotlin 官方编码规范](https://kotlinlang.org/docs/coding-style.html)。

---

## 五、代码组织

- 单文件代码量过多时（建议超过 ~300 行），**恰当地拆分成模块目录**。
- 拆分时遵循 **高聚合、低耦合** 的原则：
  - 相关功能放在同一文件或子目录下；
  - 模块之间通过清晰的接口交互，避免循环依赖。
- 添加**必要的中文注释**方便阅读理解——尤其是核心算法、布局逻辑、信号机制等不易直观理解的部分。

---

## 六、最小变更原则

- **只改与需求相关的最少代码**。不要顺手重构、不要大面积改格式、不要动无关模块。
- 每处改动都应有明确目的。如果一段代码看起来"不太优雅"但不影响当前任务，留给将来处理。
- 如果确实需要大范围重构，**先单独说明并获得确认**。

---

## 七、先写测试，再写实现

- 遵循 **TDD（测试驱动开发）**：先写测试用例，再写实现代码。
- 测试放在对应模块的正确位置：`commonTest` / `jvmTest` / `androidTest`。
- 测试用例命名清晰，覆盖正常路径和边界情况。

---

## 八、提交前验证

- 修改完成后，**必须验证**：
  1. 编译通过：`./gradlew compileKotlin`（或对应平台的编译任务）
  2. 相关测试通过：`./gradlew <module>:test`
- 不能编译的代码视为**未完成**，不得交付。

---

## 九、依赖管理

- **不要随意引入新依赖**。如需引入，先说明理由、影响范围和可能的替代方案。
- 所有依赖版本统一在 `gradle/libs.versions.toml` 中管理。
- 优先使用 Kotlin 标准库和项目已有依赖，避免功能重复的库。

---

## 十、公共 API 变更需同步文档

- 修改或新增公开 API（如 `LayoutNode`、`Signal`、`FlexLayout` 等对外暴露的接口）时，**必须同步更新 `docs/` 下对应的文档章节**。
- 确保文档与代码保持一致，方便后续使用者查阅。

---

## 十一、安全红线

- **禁止**在代码或配置中写入密钥、token、密码等敏感信息。
- 调试用的临时代码（如 `println`、`TODO()`、测试入口等）不要提交到仓库。
- 不要在代码中硬编码路径、用户名、环境相关的硬编码值。

---

## 十二、平台分离

- Kotlin Multiplatform 项目中，代码按平台严格分离：
  - `commonMain` — 放跨平台通用逻辑，只能使用通用 API。
  - `jvmMain` — 放 JVM / Desktop 平台实现。
  - `androidMain` — 放 Android 平台实现。
  - `webMain` — 放 Web 平台实现。
- **不要**在平台特定代码中引用另一平台的 API。
- 跨平台共享的接口通过 expect/actual 机制声明。

---

## 十三、项目概览

| 模块 | 作用 |
|---|---|
| `skia-engine` | 引擎核心：节点、布局、绘制、文字、输入 |
| `layout` | 布局算法：Flex / Stack / absolute |
| `signal` | 信号与 memo（反应式状态） |
| `mve` | 节点树与列表渲染 |
| `lib` | 通用工具库（Either、Kanren 等） |
| `desktopApp` | 桌面 Demo（运行入口） |
| `shared` | 跨平台共享代码 |
| `webApp` | Web 平台 Demo |
| `androidApp` | Android 平台 Demo |

更多信息请参阅 [README.md](README.md)。