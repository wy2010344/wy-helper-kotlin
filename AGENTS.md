# AGENTS.md

## 代码风格

- 新增代码与同文件、同模块风格保持一致（命名、缩进、括号）。
- 300 行以上的文件考虑拆分。高内聚低耦合。

## 最小变更

- 只改与需求相关的最少代码。不要顺手重构、不要大面积改格式。
- 需要大范围重构时先说明并获得确认。

## 提交前验证

- 编译通过 + 相关测试通过，不能编译的代码视为未完成。

## 依赖管理

- 新增依赖需说明理由。版本统一在 `gradle/libs.versions.toml` 管理。
- 优先用已有依赖，避免功能重复的库。

## 安全红线

- 禁止提交密钥、token、密码等敏感信息。
- 不要提交调试临时代码（println、TODO() 等）。
- 不要硬编码路径、用户名等环境相关值。

## 平台分离（KMP）

- `commonMain` 放跨平台通用逻辑，`jvmMain`/`androidMain`/`webMain` 放平台实现。
- 跨平台接口通过 expect/actual 声明。不要在平台代码中引用另一平台的 API。

## 项目模块

| 模块 | 作用 |
|---|---|
| `skia-engine` | 引擎核心：节点、布局、绘制、文字、输入 |
| `layout` | 布局算法：Flex / Stack / absolute |
| `signal` | 信号与 memo（反应式状态） |
| `mve` | 节点树与列表渲染 |
| `lib` | 通用工具库（Either、Kanren 等） |
| `desktopApp` | 桌面 Demo（运行入口） |
